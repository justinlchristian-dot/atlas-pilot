import { PagePlaceholder } from "@/components/page-placeholder";
import { placeholderPages } from "@/data/today";

export default function VaultPage() {
  return <PagePlaceholder {...placeholderPages.vault} />;
}
